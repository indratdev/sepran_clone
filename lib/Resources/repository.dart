import 'transaction_data.dart';

class Repository {
  // final transaction = TransactionData();

  // Future<Stream<QuerySnapshot<Object?>>> getAllTransaction(DateTime date) =>
  //     transaction.getTransactionAll(date);
}
